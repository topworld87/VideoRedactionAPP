# ONNX Runtime (DirectML)

- Package: **Microsoft.ML.OnnxRuntime.DirectML 1.20.1**
- Layout: `include/`, `lib/onnxruntime.lib`, `bin/onnxruntime.dll` (~14 MB)
- License: MIT — see `LICENSE`, `ThirdPartyNotices.txt`
- **Do not** add CUDA EP packages

Re-fetch via Clash proxy (`127.0.0.1:7897`):

```text
https://www.nuget.org/api/v2/package/Microsoft.ML.OnnxRuntime.DirectML/1.20.1
```

Extract `build/native/include/*` and `runtimes/win-x64/native/*` into this folder.
